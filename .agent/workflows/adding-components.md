---
description: When adding new React components
---

See [`apps/site/CLAUDE.md` → Adding a component](../../apps/site/CLAUDE.md#adding-a-component)
for placement, `class-variance-authority` variants, `next-intl`, `ThemeContext`,
and the test + Storybook story requirement.

Repo-wide rules — strict TypeScript, kebab-case filenames, JSDoc on exports,
copyright header — are in [`AGENTS.md` → Making a change](../../AGENTS.md#making-a-change).

Run `bun run format` from the repository root when you are done.
