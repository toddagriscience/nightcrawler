// Copyright © Todd Agriscience, Inc. All rights reserved.

/** Vitest config for `@nightcrawler/db` unit tests. */
const config = {
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
};

export default config;
